[build]
icon = "📦"
buildCommand = "cd frontend && npm install && npm run build"
startCommand = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-80}"

[variables]
PYTHON_VERSION = "3.12"
NODE_VERSION = "20"